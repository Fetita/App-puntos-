{
  "name": "anotador-de-truco",
  "version": "1.0.0",
  "private": true,
  "description": "Anotador de Truco — el anotador de truco más lindo. App web envuelta con Capacitor para App Store y Google Play.",
  "scripts": {
    "serve": "npx --yes serve www -l 5000",
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cap sync android && cd android && ./gradlew assembleRelease",
    "bundle:android": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
